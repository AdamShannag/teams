// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"project-service/repository/ent/member"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Member is the model entity for the Member schema.
type Member struct {
	config
	// ID of the ent.
	ID string `json:"memberId"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MemberQuery when eager-loading is set.
	Edges        MemberEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MemberEdges holds the relations/edges for other nodes in the graph.
type MemberEdges struct {
	// Create holds the value of the create edge.
	Create []*Project `json:"create,omitempty"`
	// Assign holds the value of the assign edge.
	Assign []*Team `json:"assign,omitempty"`
	// Add holds the value of the add edge.
	Add []*Attachment `json:"add,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// CreateOrErr returns the Create value or an error if the edge
// was not loaded in eager-loading.
func (e MemberEdges) CreateOrErr() ([]*Project, error) {
	if e.loadedTypes[0] {
		return e.Create, nil
	}
	return nil, &NotLoadedError{edge: "create"}
}

// AssignOrErr returns the Assign value or an error if the edge
// was not loaded in eager-loading.
func (e MemberEdges) AssignOrErr() ([]*Team, error) {
	if e.loadedTypes[1] {
		return e.Assign, nil
	}
	return nil, &NotLoadedError{edge: "assign"}
}

// AddOrErr returns the Add value or an error if the edge
// was not loaded in eager-loading.
func (e MemberEdges) AddOrErr() ([]*Attachment, error) {
	if e.loadedTypes[2] {
		return e.Add, nil
	}
	return nil, &NotLoadedError{edge: "add"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Member) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case member.FieldID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Member fields.
func (m *Member) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case member.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				m.ID = value.String
			}
		default:
			m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Member.
// This includes values selected through modifiers, order, etc.
func (m *Member) Value(name string) (ent.Value, error) {
	return m.selectValues.Get(name)
}

// QueryCreate queries the "create" edge of the Member entity.
func (m *Member) QueryCreate() *ProjectQuery {
	return NewMemberClient(m.config).QueryCreate(m)
}

// QueryAssign queries the "assign" edge of the Member entity.
func (m *Member) QueryAssign() *TeamQuery {
	return NewMemberClient(m.config).QueryAssign(m)
}

// QueryAdd queries the "add" edge of the Member entity.
func (m *Member) QueryAdd() *AttachmentQuery {
	return NewMemberClient(m.config).QueryAdd(m)
}

// Update returns a builder for updating this Member.
// Note that you need to call Member.Unwrap() before calling this method if this Member
// was returned from a transaction, and the transaction was committed or rolled back.
func (m *Member) Update() *MemberUpdateOne {
	return NewMemberClient(m.config).UpdateOne(m)
}

// Unwrap unwraps the Member entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (m *Member) Unwrap() *Member {
	_tx, ok := m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Member is not a transactional entity")
	}
	m.config.driver = _tx.drv
	return m
}

// String implements the fmt.Stringer.
func (m *Member) String() string {
	var builder strings.Builder
	builder.WriteString("Member(")
	builder.WriteString(fmt.Sprintf("id=%v", m.ID))
	builder.WriteByte(')')
	return builder.String()
}

// Members is a parsable slice of Member.
type Members []*Member
